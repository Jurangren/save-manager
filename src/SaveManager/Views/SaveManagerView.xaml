<UserControl x:Class="SaveManager.Views.SaveManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SaveManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource PopupBackgroundBrush}">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" />
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,2"/>
        </Style>
        <Style x:Key="HeaderText" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="20" FontWeight="Bold">
                <Run Text="🎮 "/>
                <Run Text="{Binding GameName, Mode=OneWay}"/>
                <Run Text=" - 存档管理"/>
            </TextBlock>
            <TextBlock Text="{Binding GameId, StringFormat='游戏ID: {0}'}" 
                       Opacity="0.6" FontSize="11" Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- 主要内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：存档路径配置 -->
            <Border Grid.Column="0" Style="{StaticResource SectionBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Style="{StaticResource HeaderText}" Text="📂 存档路径配置"/>
                    
                    <!-- 添加按钮 -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Command="{Binding AddFolderCommand}" ToolTip="添加存档文件夹">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁" Margin="0,0,6,0"/>
                                <TextBlock Text="添加文件夹"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddFileCommand}" ToolTip="添加单个存档文件">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" Margin="0,0,6,0"/>
                                <TextBlock Text="添加文件"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    
                    <!-- 路径列表 -->
                    <Border Grid.Row="2" BorderBrush="{DynamicResource NormalBorderBrush}" 
                            BorderThickness="1" CornerRadius="4">
                        <ListBox ItemsSource="{Binding SavePaths}" 
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" Text="{Binding TypeIcon}" 
                                                   FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Path}" FontSize="10" Opacity="0.7" 
                                                       TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}"/>
                                        </StackPanel>
                                        
                                        <Button Grid.Column="2" 
                                                Command="{Binding DataContext.RemovePathCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}"
                                                Content="✕" 
                                                ToolTip="移除"
                                                Padding="8,4"
                                                VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Style.Triggers>
                                        <Trigger Property="HasItems" Value="False">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <TextBlock Text="未配置存档路径&#x0a;请点击上方按钮添加存档文件夹或文件" 
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"
                                                                   TextAlignment="Center"
                                                                   Opacity="0.5"
                                                                   Margin="20"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                        </ListBox>
                    </Border>
                    
                    <!-- 保存配置按钮 -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Command="{Binding ImportConfigCommand}" Margin="0,0,10,0" ToolTip="导入其他游戏的路径配置">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" Margin="0,0,6,0"/>
                                <TextBlock Text="导入配置"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding ExportConfigCommand}" Margin="0,0,10,0" ToolTip="导出当前路径配置">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📤" Margin="0,0,6,0"/>
                                <TextBlock Text="导出配置"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding SaveConfigCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💾" Margin="0,0,6,0"/>
                                <TextBlock Text="保存路径配置"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 右侧：备份管理 -->
            <Border Grid.Column="2" Style="{StaticResource SectionBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Style="{StaticResource HeaderText}" Text="💾 备份管理"/>
                    
                    <!-- 创建新备份 -->
                    <Border Grid.Row="1" Background="{DynamicResource ExpanderBackgroundBrush}" 
                            CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="创建新备份" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding NewBackupDescription, UpdateSourceTrigger=PropertyChanged}" 
                                     Height="60" TextWrapping="Wrap" AcceptsReturn="True"
                                     ToolTip="输入备份备注（可选）"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                <Button Command="{Binding ImportBackupCommand}" Margin="0,0,10,0" ToolTip="从外部文件导入备份(ZIP)">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📥" Margin="0,0,6,0"/>
                                        <TextBlock Text="导入"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding CreateBackupCommand}" 
                                        IsEnabled="{Binding HasSavePaths}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📦" Margin="0,0,6,0"/>
                                        <TextBlock Text="创建备份"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- 备份列表 -->
                    <Border Grid.Row="2" BorderBrush="{DynamicResource NormalBorderBrush}" 
                            BorderThickness="1" CornerRadius="4">
                        <ListBox ItemsSource="{Binding Backups}" 
                                 SelectedItem="{Binding SelectedBackup}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource NormalBorderBrush}" 
                                            BorderThickness="0,0,0,1" Padding="8" Margin="0,0,0,4">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                                       Text="{Binding Name}" FontWeight="Bold"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                                       Text="{Binding FormattedSize}" 
                                                       Opacity="0.7" FontSize="11"/>
                                            
                                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                                                       Text="{Binding FormattedDate}" 
                                                       FontSize="11" Opacity="0.6"/>
                                            
                                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" 
                                                       Text="{Binding Description}" 
                                                       TextWrapping="Wrap"
                                                       Visibility="{Binding Description, Converter={x:Static local:StringToVisibilityConverter.Instance}}"
                                                       Opacity="0.8" FontStyle="Italic" Margin="0,4,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Style.Triggers>
                                        <Trigger Property="HasItems" Value="False">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate>
                                                        <TextBlock Text="暂无备份&#x0a;配置存档路径后创建第一个备份" 
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"
                                                                   TextAlignment="Center"
                                                                   Opacity="0.5"
                                                                   Margin="20"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                        </ListBox>
                    </Border>
                    
                    <!-- 操作按钮 -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Command="{Binding EditBackupNoteCommand}" 
                                IsEnabled="{Binding IsBackupSelected}"
                                ToolTip="修改选中备份的备注">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✏️" Margin="0,0,6,0"/>
                                <TextBlock Text="备注"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding RestoreBackupCommand}" 
                                IsEnabled="{Binding IsBackupSelected}"
                                ToolTip="还原选中的备份">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="↩️" Margin="0,0,6,0"/>
                                <TextBlock Text="还原"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding DeleteBackupCommand}" 
                                IsEnabled="{Binding IsBackupSelected}"
                                ToolTip="删除选中的备份">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🗑️" Margin="0,0,6,0"/>
                                <TextBlock Text="删除"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding OpenBackupFolderCommand}" ToolTip="打开备份文件夹">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📂" Margin="0,0,6,0"/>
                                <TextBlock Text="打开文件夹"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
